---
  name: "send_message"
  runner_type: "python-script"
  description: "send a message to a whatsapp user or group via chat-api.com"
  enabled: true
  entry_point: "send_message.py"
  parameters:
    message:
      type: "string"
      description: "message to send"
      required: true
    chatId:
      type: "string"
      description: "chatId to send the message"
      required: true